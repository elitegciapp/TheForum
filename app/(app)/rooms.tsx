import { Screen } from '../../components/Screen';
import { RoomList } from '../../components/RoomList';

export default function Rooms() {
  return (
    <Screen>
      <RoomList variant="screen" />
    </Screen>
  );
}
